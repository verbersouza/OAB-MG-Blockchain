<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Blog · Comissão de Blockchain, Criptoativos e Tokenização · OAB/MG</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Syne:wght@400;500;600;700;800&family=DM+Mono:wght@300;400&display=swap" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/marked/9.1.6/marked.min.js"></script>
<style>
  :root {
    --bg: #080c10;
    --bg2: #0c1118;
    --bg3: #111820;
    --surface: rgba(255,255,255,0.03);
    --border: rgba(255,255,255,0.07);
    --border-accent: rgba(100,220,200,0.2);
    --accent: #64dcc8;
    --accent2: #3a9e8e;
    --accent-glow: rgba(100,220,200,0.12);
    --text: #e8edf2;
    --text-muted: #6b7a8a;
    --text-dim: #3d4a57;
    --gold: #c9a96e;
  }
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    background: var(--bg);
    color: var(--text);
    font-family: 'Syne', sans-serif;
    font-size: 16px;
    line-height: 1.6;
    overflow-x: hidden;
  }
  body::before {
    content: '';
    position: fixed; inset: 0;
    background-image:
      linear-gradient(rgba(100,220,200,0.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(100,220,200,0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    pointer-events: none; z-index: 0;
  }
  nav, main, footer { position: relative; z-index: 1; }

  /* NAV */
  nav {
    position: fixed; top: 0; left: 0; right: 0; z-index: 100;
    display: flex; align-items: center; justify-content: space-between;
    padding: 0 48px; height: 72px;
    background: rgba(8,12,16,0.9);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
  }
  .nav-brand { display: flex; align-items: center; gap: 14px; text-decoration: none; }
  .nav-logo { width: 36px; height: 36px; object-fit: contain; display: block; }
  .nav-title { font-size: 12px; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); line-height: 1.3; }
  .nav-title span { display: block; color: var(--text); }
  .nav-links { display: flex; gap: 36px; list-style: none; }
  .nav-links a { font-size: 12px; font-weight: 500; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); text-decoration: none; transition: color 0.2s; }
  .nav-links a:hover { color: var(--accent); }
  .nav-links a.nav-blog {
    color: var(--accent); border: 1px solid var(--border-accent);
    padding: 6px 14px; font-size: 11px; letter-spacing: 0.12em;
  }
  .nav-links a.nav-blog:hover { background: var(--accent-glow); }

  /* HAMBURGER */
  .hamburger {
    display: none; flex-direction: column; justify-content: center;
    gap: 5px; background: none; border: none; cursor: pointer; padding: 4px; z-index: 110;
  }
  .hamburger span { display: block; width: 24px; height: 1.5px; background: var(--text); transition: all 0.3s; }
  .hamburger.open span:nth-child(1) { transform: translateY(6.5px) rotate(45deg); }
  .hamburger.open span:nth-child(2) { opacity: 0; }
  .hamburger.open span:nth-child(3) { transform: translateY(-6.5px) rotate(-45deg); }

  /* MOBILE MENU */
  .mobile-menu {
    display: none; position: fixed; top: 0; right: 0;
    width: 280px; height: 100vh; background: var(--bg2);
    border-left: 1px solid var(--border); z-index: 200;
    flex-direction: column; padding: 80px 40px 40px;
    transform: translateX(100%); transition: transform 0.35s ease;
  }
  .mobile-menu.open { transform: translateX(0); }
  .mobile-menu ul { list-style: none; display: flex; flex-direction: column; }
  .mobile-menu ul li { border-bottom: 1px solid var(--border); }
  .mobile-menu ul a {
    display: block; padding: 18px 0;
    font-family: 'Syne', sans-serif; font-size: 15px; font-weight: 600;
    letter-spacing: 0.08em; text-transform: uppercase;
    color: var(--text-muted); text-decoration: none; transition: color 0.2s;
  }
  .mobile-menu ul a:hover, .mobile-menu ul a.mobile-blog { color: var(--accent); }
  .mobile-menu-close {
    position: absolute; top: 24px; right: 24px;
    background: none; border: none; color: var(--text-muted);
    font-size: 20px; cursor: pointer; transition: color 0.2s;
  }
  .mobile-overlay {
    display: none; position: fixed; inset: 0;
    background: rgba(0,0,0,0.6); z-index: 190; backdrop-filter: blur(4px);
  }
  .mobile-overlay.open { display: block; }

  /* MAIN */
  main { padding: 120px 48px 80px; max-width: 1100px; margin: 0 auto; }

  /* BLOG HEADER */
  .blog-header { margin-bottom: 72px; }
  .blog-label {
    font-family: 'DM Mono', monospace; font-size: 11px; letter-spacing: 0.18em;
    text-transform: uppercase; color: var(--accent); margin-bottom: 20px;
    display: flex; align-items: center; gap: 12px;
  }
  .blog-label::after { content: ''; width: 40px; height: 1px; background: var(--accent); }
  .blog-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(36px, 4vw, 56px); font-weight: 300; line-height: 1.1;
  }
  .blog-title em { font-style: italic; color: var(--gold); }

  /* LOADING / ERROR */
  .status {
    font-family: 'DM Mono', monospace; font-size: 12px; color: var(--text-muted);
    padding: 48px 0; text-align: center;
  }
  .status.error { color: #e07070; }

  /* POST LIST */
  .post-list { display: flex; flex-direction: column; gap: 1px; background: var(--border); border: 1px solid var(--border); }
  .post-card {
    background: var(--bg); padding: 36px 40px; cursor: pointer;
    transition: background 0.3s; position: relative; overflow: hidden;
  }
  .post-card::before {
    content: ''; position: absolute; inset: 0;
    background: linear-gradient(135deg, var(--accent-glow) 0%, transparent 60%);
    opacity: 0; transition: opacity 0.3s;
  }
  .post-card:hover { background: var(--bg2); }
  .post-card:hover::before { opacity: 1; }
  .post-meta {
    display: flex; align-items: center; gap: 16px; margin-bottom: 14px; flex-wrap: wrap;
  }
  .post-date {
    font-family: 'DM Mono', monospace; font-size: 11px;
    color: var(--text-dim); letter-spacing: 0.08em;
  }
  .post-tag {
    font-family: 'DM Mono', monospace; font-size: 10px;
    padding: 2px 8px; border: 1px solid var(--border-accent);
    color: var(--accent); letter-spacing: 0.05em; background: var(--accent-glow);
  }
  .post-card-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(22px, 2.5vw, 32px); font-weight: 400; line-height: 1.2;
    margin-bottom: 10px; transition: color 0.3s;
  }
  .post-card:hover .post-card-title { color: var(--accent); }
  .post-author {
    font-family: 'DM Mono', monospace; font-size: 11px; color: var(--text-dim);
  }
  .post-arrow {
    position: absolute; right: 40px; top: 50%;
    transform: translateY(-50%);
    font-size: 20px; color: var(--text-dim); transition: all 0.3s;
  }
  .post-card:hover .post-arrow { color: var(--accent); transform: translateY(-50%) translateX(4px); }

  /* POST READER */
  .post-reader { display: none; }
  .post-reader.active { display: block; }
  .post-list-view { display: block; }
  .post-list-view.hidden { display: none; }

  .back-btn {
    display: inline-flex; align-items: center; gap: 10px;
    font-family: 'DM Mono', monospace; font-size: 11px;
    letter-spacing: 0.1em; text-transform: uppercase;
    color: var(--text-muted); background: none; border: none;
    cursor: pointer; padding: 0; margin-bottom: 48px;
    transition: color 0.2s;
  }
  .back-btn:hover { color: var(--accent); }

  .post-reader-meta { margin-bottom: 48px; padding-bottom: 32px; border-bottom: 1px solid var(--border); }
  .post-reader-label {
    font-family: 'DM Mono', monospace; font-size: 11px; letter-spacing: 0.15em;
    text-transform: uppercase; color: var(--accent); margin-bottom: 16px;
    display: flex; align-items: center; gap: 10px;
  }
  .post-reader-label::after { content: ''; width: 32px; height: 1px; background: var(--accent); }
  .post-reader-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(32px, 4vw, 60px); font-weight: 300; line-height: 1.05;
    margin-bottom: 20px;
  }
  .post-reader-byline {
    font-family: 'DM Mono', monospace; font-size: 11px; color: var(--text-dim);
    display: flex; gap: 20px; flex-wrap: wrap;
  }

  /* MARKDOWN CONTENT */
  .post-content { max-width: 720px; }
  .post-content h2 {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(24px, 2.5vw, 36px); font-weight: 400; line-height: 1.2;
    color: var(--text); margin: 56px 0 20px; padding-top: 24px;
    border-top: 1px solid var(--border);
  }
  .post-content h3 {
    font-family: 'Syne', sans-serif; font-size: 16px; font-weight: 700;
    color: var(--accent); margin: 32px 0 12px; letter-spacing: 0.05em;
    text-transform: uppercase;
  }
  .post-content p {
    font-size: 16px; color: var(--text-muted); line-height: 1.85;
    margin-bottom: 20px;
  }
  .post-content strong { color: var(--text); font-weight: 600; }
  .post-content em { font-style: italic; color: var(--text); }
  .post-content blockquote {
    border-left: 2px solid var(--accent2); padding: 14px 20px;
    margin: 24px 0; background: rgba(100,220,200,0.04);
    font-size: 15px; color: var(--text-muted); line-height: 1.7;
  }
  .post-content blockquote p { margin-bottom: 0; }
  .post-content ul, .post-content ol {
    margin: 16px 0 20px 24px; color: var(--text-muted);
    font-size: 16px; line-height: 1.8;
  }
  .post-content li { margin-bottom: 6px; }
  .post-content code {
    font-family: 'DM Mono', monospace; font-size: 13px;
    background: var(--bg3); padding: 2px 6px; border-radius: 3px;
    color: var(--accent);
  }
  .post-content hr {
    border: none; border-top: 1px solid var(--border); margin: 48px 0;
  }
  .post-content a { color: var(--accent); text-decoration: underline; text-underline-offset: 3px; }
  .post-content a:hover { color: #7de8d4; }

  /* FOOTER */
  footer {
    background: var(--bg); border-top: 1px solid var(--border);
    padding: 32px 48px; margin-top: 80px;
    display: flex; justify-content: space-between; align-items: center;
    flex-wrap: wrap; gap: 16px;
  }
  .footer-brand {
    font-family: 'Cormorant Garamond', serif; font-size: 16px;
    font-weight: 300; color: var(--text-muted);
  }
  .footer-disclaimer {
    font-family: 'DM Mono', monospace; font-size: 10px;
    color: var(--text-dim); max-width: 400px; line-height: 1.6;
  }
  .footer-copy { font-family: 'DM Mono', monospace; font-size: 10px; color: var(--text-dim); }

  @media (max-width: 900px) {
    nav { padding: 0 24px; }
    .nav-links { display: none; }
    .hamburger { display: flex; }
    .mobile-menu { display: flex; }
    main { padding: 100px 24px 60px; }
    .post-card { padding: 28px 24px; }
    .post-arrow { display: none; }
    footer { padding: 28px 24px; flex-direction: column; align-items: flex-start; }
  }
</style>
</head>
<body>

<!-- NAVBAR -->
<nav id="navbar">
  <a href="/" class="nav-brand">
    <img src="/logo.png" alt="OAB/MG" class="nav-logo">
    <div class="nav-title">
      <span>Comissão de Blockchain, Criptoativos e Tokenização</span>
      OAB Minas Gerais
    </div>
  </a>
  <ul class="nav-links">
    <li><a href="/#marco">O Marco</a></li>
    <li><a href="/#temas">Temas</a></li>
    <li><a href="/#glossario">Glossário</a></li>
    <li><a href="/#comissao">A Comissão</a></li>
    <li><a href="/blog" class="nav-blog">Blog</a></li>
  </ul>
  <button class="hamburger" id="hamburger" onclick="toggleMenu()" aria-label="Menu">
    <span></span><span></span><span></span>
  </button>
</nav>

<!-- Mobile overlay -->
<div class="mobile-menu" id="mobileMenu">
  <button class="mobile-menu-close" onclick="closeMenu()">✕</button>
  <ul>
    <li><a href="/#marco" onclick="closeMenu()">O Marco</a></li>
    <li><a href="/#temas" onclick="closeMenu()">Temas</a></li>
    <li><a href="/#glossario" onclick="closeMenu()">Glossário</a></li>
    <li><a href="/#comissao" onclick="closeMenu()">A Comissão</a></li>
    <li><a href="/blog" class="mobile-blog">Blog</a></li>
  </ul>
</div>
<div class="mobile-overlay" id="mobileOverlay" onclick="closeMenu()"></div>

<main>
  <!-- LIST VIEW -->
  <div class="post-list-view" id="listView">
    <div class="blog-header">
      <div class="blog-label">Blog · Comissão OAB/MG</div>
      <h1 class="blog-title">Publicações e <em>análises</em></h1>
    </div>
    <div id="postList"></div>
  </div>

  <!-- POST READER -->
  <div class="post-reader" id="postReader">
    <button class="back-btn" onclick="showList()">← Voltar ao Blog</button>
    <div class="post-reader-meta">
      <div class="post-reader-label" id="readerCategory">Educacional</div>
      <h1 class="post-reader-title" id="readerTitle"></h1>
      <div class="post-reader-byline">
        <span id="readerAuthor"></span>
        <span id="readerDate"></span>
      </div>
    </div>
    <div class="post-content" id="readerContent"></div>
  </div>
</main>

<footer>
  <div class="footer-brand">Comissão de Blockchain, Criptoativos e Tokenização · OAB/MG</div>
  <div class="footer-disclaimer">Conteúdo informativo e educacional. Não constitui parecer jurídico.</div>
  <div class="footer-copy">© 2026 OAB · Minas Gerais</div>
</footer>

<script>
  const REPO = 'verbersouza/OAB-MG-Blockchain';
  const POSTS_PATH = 'posts';
  const API = `https://api.github.com/repos/${REPO}/contents/${POSTS_PATH}`;

  // ── Frontmatter parser ──
  function parseFrontmatter(raw) {
    const match = raw.match(/^---\n([\s\S]*?)\n---\n([\s\S]*)$/);
    if (!match) return { meta: {}, body: raw };
    const meta = {};
    match[1].split('\n').forEach(line => {
      const [key, ...val] = line.split(':');
      if (key && val.length) {
        meta[key.trim()] = val.join(':').trim().replace(/^"|"$/g, '');
      }
    });
    return { meta, body: match[2] };
  }

  // ── Format date ──
  function formatDate(str) {
    if (!str) return '';
    const [y, m, d] = str.split('-');
    const months = ['Jan','Fev','Mar','Abr','Mai','Jun','Jul','Ago','Set','Out','Nov','Dez'];
    return `${d} ${months[parseInt(m)-1]} ${y}`;
  }

  // ── Fetch post list ──
  async function loadPosts() {
    const container = document.getElementById('postList');
    container.innerHTML = '<div class="status">Carregando publicações...</div>';
    try {
      const res = await fetch(API);
      if (!res.ok) throw new Error(`GitHub API: ${res.status}`);
      const files = await res.json();
      const mdFiles = files.filter(f => f.name.endsWith('.md'));

      if (mdFiles.length === 0) {
        container.innerHTML = '<div class="status">Nenhuma publicação encontrada ainda.</div>';
        return;
      }

      // Fetch all posts in parallel
      const posts = await Promise.all(mdFiles.map(async file => {
        const r = await fetch(file.url);
        const data = await r.json();
        const raw = atob(data.content.replace(/\n/g,''));
        const decoded = decodeURIComponent(escape(raw));
        const { meta, body } = parseFrontmatter(decoded);
        return { meta, body, filename: file.name };
      }));

      // Sort by date descending
      posts.sort((a, b) => {
        const da = a.meta.date || '0000-00-00';
        const db = b.meta.date || '0000-00-00';
        return db.localeCompare(da);
      });

      // Render cards
      container.innerHTML = '<div class="post-list">' +
        posts.map((post, i) => `
          <div class="post-card" onclick="openPost(${i})" data-index="${i}">
            <div class="post-meta">
              <span class="post-date">${formatDate(post.meta.date)}</span>
              ${post.meta.categoria ? `<span class="post-tag">${post.meta.categoria}</span>` : ''}
            </div>
            <div class="post-card-title">${post.meta.title || post.filename}</div>
            <div class="post-author">${post.meta.author ? 'Por ' + post.meta.author : ''}</div>
            <span class="post-arrow">→</span>
          </div>
        `).join('') +
      '</div>';

      // Store posts globally for reader
      window.__posts = posts;

    } catch (err) {
      container.innerHTML = `<div class="status error">Erro ao carregar publicações.<br><small>${err.message}</small></div>`;
    }
  }

  // ── Open post reader ──
  function openPost(index) {
    const post = window.__posts[index];
    if (!post) return;

    document.getElementById('readerTitle').textContent = post.meta.title || '';
    document.getElementById('readerAuthor').textContent = post.meta.author ? 'Por ' + post.meta.author : '';
    document.getElementById('readerDate').textContent = formatDate(post.meta.date);
    document.getElementById('readerCategory').textContent = post.meta.categoria || 'Publicação';

    // Remove frontmatter header lines from body before rendering
    const cleanBody = post.body
      .replace(/^#\s.*\n/, '')
      .replace(/^\*\*Autora:\*\*.*\n/m, '')
      .replace(/^\*\*Categoria:\*\*.*\n/m, '')
      .replace(/^---\n/m, '')
      .trim();

    document.getElementById('readerContent').innerHTML = marked.parse(cleanBody);

    document.getElementById('listView').classList.add('hidden');
    document.getElementById('postReader').classList.add('active');
    window.scrollTo(0, 0);
  }

  // ── Back to list ──
  function showList() {
    document.getElementById('listView').classList.remove('hidden');
    document.getElementById('postReader').classList.remove('active');
    window.scrollTo(0, 0);
  }

  // ── Mobile menu ──
  function toggleMenu() {
    const menu = document.getElementById('mobileMenu');
    const overlay = document.getElementById('mobileOverlay');
    const btn = document.getElementById('hamburger');
    const isOpen = menu.classList.contains('open');
    if (isOpen) { closeMenu(); } else {
      menu.classList.add('open');
      overlay.classList.add('open');
      btn.classList.add('open');
    }
  }
  function closeMenu() {
    document.getElementById('mobileMenu').classList.remove('open');
    document.getElementById('mobileOverlay').classList.remove('open');
    document.getElementById('hamburger').classList.remove('open');
  }

  // ── Init ──
  loadPosts();
</script>
</body>
</html>
